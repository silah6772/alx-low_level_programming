#include <stdio.h>

int main() {
	   int num,alph;
	   	for (num = 0; num < 10; num++)
					putchar((num % 10) + '0');

			for (alph = 97; alph <= 102; alph++)
						putchar(alph);

				putchar('\n');

					return (0);}

